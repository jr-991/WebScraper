[project]
name = "realestate-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "fastapi[standard]>=0.128.8",
    "requests>=2.32.0",
]

[project.scripts]
realestate-api = "realestate_api:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
